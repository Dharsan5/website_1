# Prisma — React + Vite

Converted the original static HTML into a modern React + Vite project with Tailwind CSS.

Quick start:

1. Install dependencies

```bash
npm install
```

2. Start dev server

```bash
npm run dev
```

Build:

```bash
npm run build
npm run preview
```
